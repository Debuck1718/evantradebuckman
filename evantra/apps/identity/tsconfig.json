{
  "extends": "../../tsconfig.base.json",

  "compilerOptions": {
    "rootDir": ".",
    "outDir": "./dist"
  },

  "include": [
    "src/**/*.ts",
    "infrastructure/**/*.ts",
    "playground/**/*.ts"
  ],

  "exclude": [
    "dist",
    "node_modules"
  ]
}